<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="student.sdu.hearingscreening">

    <application
        android:name=".application.HearingScreeningApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainMenuActivity" />
        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HowToActivity" />
        <activity android:name=".activities.HowTo2Activity" />
        <activity android:name=".activities.Test1Activity" />
        <activity android:name=".activities.ResultsActivity" />
        <activity android:name=".activities.EarlierResultsActivity" />
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.SendResultsActivity" />
        <activity android:name=".activities.CalibrateActivity"></activity>
    </application>

</manifest>